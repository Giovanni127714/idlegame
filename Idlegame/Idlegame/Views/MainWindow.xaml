<Window x:Class="Idlegame.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Idlegame.ViewModels"
        mc:Ignorable="d"
        Title="Idlegame" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding CurrencyDisplay}"
                       FontSize="36"
                       FontWeight="Bold"
                       HorizontalAlignment="Center" />
            <TextBlock HorizontalAlignment="Center"
                       FontSize="16"
                       Margin="0,8,0,0">
                <Run Text="{Binding IncomePerSecondDisplay, Mode=OneWay}" />
                <Run Text=" / sec" />
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
